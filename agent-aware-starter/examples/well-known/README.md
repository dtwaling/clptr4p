# Generic site well-known fixture

Example AAA files for a site that declares an HTTP `action_root`.

- `live` is `false`
- `action_root` is `/api/v1/agent-actions`

These files are fixtures, not a published origin.
