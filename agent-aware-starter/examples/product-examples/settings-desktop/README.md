# Settings-desktop product example

Illustrative AAA files for a desktop agent product with **no public HTTP action API**.

- `action_root` is the string `none`
- Mutations stay in Settings UI plus human confirmation
- Deep links use only documented anchors: `app://settings?id=<anchor>`
- Example anchors: privacy, models, connectors, appearance, about

These files are fixtures (`live: false`).
