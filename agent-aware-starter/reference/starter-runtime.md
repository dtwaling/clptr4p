# Reference — starter runtime (historical)

The historical starter at `sierracatalina/agent-aware-starter` served `public/.well-known/*` through a small Express middleware (`middleware/agent-handler.js`) and `server.js`.

That runtime is prior art, not a required dependency of AAA/0.1. This repository is protocol, schemas, and examples. A reference server is optional.
